# ============================================
# PitStop - Variaveis de Ambiente para Producao
# ============================================
# Copie este arquivo para .env e preencha os valores

# === BANCO DE DADOS ===
DB_USER=pitstop
DB_PASSWORD=SUA_SENHA_FORTE_AQUI

# === JWT (OBRIGATORIO) ===
# Gere uma chave segura com: openssl rand -base64 64
JWT_SECRET=COLE_AQUI_UMA_CHAVE_DE_64_CARACTERES_OU_MAIS
JWT_ACCESS_EXPIRATION=900000
JWT_REFRESH_EXPIRATION=604800000

# === GITHUB (para pull das imagens) ===
GITHUB_USER=seu-usuario-github

# === EMAIL (SMTP) ===
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=seu-email@gmail.com
MAIL_PASSWORD=sua-senha-de-app-google

# === EVOLUTION API (WhatsApp) - Opcional ===
EVOLUTION_API_KEY=sua-chave-evolution

# === IA (Anthropic) - Opcional ===
# PITSTOP_IA_API_KEY=sua-chave-anthropic
# PITSTOP_IA_ENCRYPTION_KEY=chave-criptografia-32-chars

# === URLs do Frontend (usadas no build) ===
VITE_API_URL=https://app.pitstopai.com.br/api
VITE_WS_URL=wss://app.pitstopai.com.br/ws
VITE_API_BASE_URL=https://app.pitstopai.com.br
