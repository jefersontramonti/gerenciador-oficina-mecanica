# ==============================================================================
# PITSTOP - ENVIRONMENT VARIABLES
# ==============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file to version control!
# ==============================================================================

# ==============================================================================
# DATABASE CONFIGURATION (Neon PostgreSQL 17)
# ==============================================================================
# Get credentials from: https://console.neon.tech/
DATABASE_URL=jdbc:postgresql://your-host.neon.tech/your-database?sslmode=require&channel_binding=require
DATABASE_USERNAME=your_username
DATABASE_PASSWORD=your_password

# ==============================================================================
# REDIS CONFIGURATION
# ==============================================================================
# Development: Use Docker Compose (localhost:6379)
# Production: Use managed Redis service (e.g., DigitalOcean, AWS ElastiCache)
REDIS_HOST=localhost
REDIS_PORT=6379

# ==============================================================================
# JWT CONFIGURATION
# ==============================================================================
# IMPORTANT: Generate a secure 256-bit (64 characters) Base64-encoded secret key
#
# How to generate a secure secret:
# - Linux/Mac:   openssl rand -base64 64
# - Windows PowerShell: [Convert]::ToBase64String((1..64 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
# - Online:      https://generate-secret.vercel.app/64
#
# NEVER use a weak secret in production!
JWT_SECRET=your-256-bit-base64-secret-key-here-CHANGE-THIS-IN-PRODUCTION-use-openssl-rand-base64-64

# ==============================================================================
# APPLICATION CONFIGURATION
# ==============================================================================
# Spring Profile (dev, prod)
SPRING_PROFILES_ACTIVE=dev

# ==============================================================================
# CORS CONFIGURATION (Configured in application.yml)
# ==============================================================================
# Frontend URLs: http://localhost:3000 (React), http://localhost:5173 (Vite)
# Production: Add your actual frontend domain in application.yml

# ==============================================================================
# OPTIONAL - EMAIL CONFIGURATION (Future features)
# ==============================================================================
# Development: Use Mailtrap (https://mailtrap.io/)
# Production: Use AWS SES, SendGrid, or similar service
# MAIL_HOST=smtp.mailtrap.io
# MAIL_PORT=2525
# MAIL_USERNAME=your_mailtrap_username
# MAIL_PASSWORD=your_mailtrap_password
# MAIL_FROM=noreply@pitstop.com

# ==============================================================================
# OPTIONAL - INTEGRATION SERVICES (Future features)
# ==============================================================================
# Mercado Pago (Payment Gateway)
# MERCADOPAGO_ACCESS_TOKEN=your_mercadopago_access_token
# MERCADOPAGO_PUBLIC_KEY=your_mercadopago_public_key

# Twilio (WhatsApp Notifications)
# TWILIO_ACCOUNT_SID=your_twilio_account_sid
# TWILIO_AUTH_TOKEN=your_twilio_auth_token
# TWILIO_WHATSAPP_FROM=whatsapp:+14155238886

# AWS SES (Email Notifications in Production)
# AWS_SES_ACCESS_KEY=your_aws_access_key
# AWS_SES_SECRET_KEY=your_aws_secret_key
# AWS_SES_REGION=us-east-1

# ==============================================================================
# SAAS MULTI-TENANT CONFIGURATION (Future - Phase 3)
# ==============================================================================
# When migrating to SaaS, uncomment and configure:
# TENANT_MODE=multi
# DEFAULT_TENANT_ID=
# TENANT_ISOLATION_LEVEL=schema  # Options: schema, row, database

# ==============================================================================
# SECURITY NOTES
# ==============================================================================
# 1. NEVER commit .env file to Git! (Already in .gitignore)
# 2. JWT_SECRET must be at least 512 bits (64 Base64 characters) for HS512
# 3. In production, use environment variables or secrets manager (AWS Secrets Manager, Vault)
# 4. REDIS: In production, enable authentication (requirepass) and use TLS
# 5. DATABASE: Neon provides automatic connection pooling and SSL by default
# ==============================================================================
