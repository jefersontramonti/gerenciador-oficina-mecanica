#===============================================================================
# PitStop AI - Template de Variáveis de Ambiente (Produção)
#
# INSTRUÇÕES:
# 1. Copie este arquivo para .env na VPS (/opt/pitstop/.env)
# 2. Substitua todos os valores <PLACEHOLDER> pelos valores reais
# 3. NUNCA commite o arquivo .env com valores reais
#===============================================================================

#--- DOMÍNIOS ---
# IMPORTANTE: DOMAIN_API deve ser o mesmo domínio do frontend para cookies funcionarem no mobile
DOMAIN_MAIN=pitstopai.com.br
DOMAIN_APP=app.pitstopai.com.br
DOMAIN_API=app.pitstopai.com.br
DOMAIN_WHATSAPP=whatsapp.pitstopai.com.br
SSL_EMAIL=noreply@pitstopai.com.br

#--- URLs DA APLICAÇÃO ---
# IMPORTANTE: Todas as URLs devem usar DOMAIN_APP (não DOMAIN_API separado)
APP_FRONTEND_URL=https://app.pitstopai.com.br
APP_BASE_URL=https://app.pitstopai.com.br
CORS_ALLOWED_ORIGINS=https://app.pitstopai.com.br,https://pitstopai.com.br

#--- COOKIE (Cross-subdomain auth) ---
# Deixar vazio para same-origin (recomendado com proxy)
APP_COOKIE_DOMAIN=

#--- FILE STORAGE ---
# Caminho para uploads de arquivos (anexos)
# Será montado como volume Docker: /var/pitstop/uploads:/var/pitstop/uploads
# O backup automático da Contabo cobre este diretório
APP_STORAGE_BASE_PATH=/var/pitstop/uploads

#--- POSTGRESQL ---
POSTGRES_PITSTOP_DB=pitstop_db
POSTGRES_PITSTOP_USER=pitstop
POSTGRES_PITSTOP_PASSWORD=<GERAR_SENHA_SEGURA>
DATABASE_URL=jdbc:postgresql://pitstop-postgres:5432/pitstop_db
DATABASE_USERNAME=pitstop
DATABASE_PASSWORD=<MESMA_SENHA_POSTGRES>

#--- REDIS ---
REDIS_HOST=pitstop-redis
REDIS_PORT=6379
REDIS_PASSWORD=<GERAR_SENHA_SEGURA>

#--- JWT (gerar com: openssl rand -base64 64) ---
JWT_SECRET=<GERAR_CHAVE_BASE64_64_BYTES>

#--- SPRING ---
SPRING_PROFILES_ACTIVE=prod

#--- EMAIL (SMTP) ---
MAIL_HOST=smtp.hostinger.com
MAIL_PORT=465
MAIL_USERNAME=<EMAIL_SMTP>
MAIL_PASSWORD=<SENHA_EMAIL>
MAIL_FROM_NAME=PitStop
MAIL_SMTP_AUTH=true
MAIL_SMTP_SSL_ENABLE=true
MAIL_SMTP_STARTTLS_ENABLE=false

#--- EVOLUTION API (WhatsApp) ---
EVOLUTION_API_URL=https://whatsapp.pitstopai.com.br
EVOLUTION_API_KEY=<CHAVE_EVOLUTION_API>

#--- INTEGRAÇÕES OPCIONAIS ---
# Mercado Pago
# MERCADOPAGO_WEBHOOK_SECRET=<SECRET_WEBHOOK>

# Anthropic (IA)
# ANTHROPIC_API_KEY=<CHAVE_API>

# Telegram
# TELEGRAM_BOT_TOKEN=<TOKEN_BOT>
# TELEGRAM_CHAT_ID=<CHAT_ID>
