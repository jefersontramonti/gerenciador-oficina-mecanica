#===============================================================================
# PitStop AI - Docker Compose (Produção)
# Sincronizado com VPS: /opt/pitstop/docker-compose.yml
#
# IMPORTANTE: Este arquivo deve ser copiado para /opt/pitstop/docker-compose.yml
# na VPS junto com as pastas backend/ e frontend/
#===============================================================================

services:
  #--- POSTGRESQL PITSTOP ---
  postgres-pitstop:
    image: postgres:16-alpine
    container_name: pitstop-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_PITSTOP_DB}
      POSTGRES_USER: ${POSTGRES_PITSTOP_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PITSTOP_PASSWORD}
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - /opt/pitstop/data/postgres-pitstop:/var/lib/postgresql/data
    ports:
      - "127.0.0.1:5432:5432"
    networks:
      - pitstop-app-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_PITSTOP_USER} -d ${POSTGRES_PITSTOP_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  #--- REDIS ---
  redis:
    image: redis:7-alpine
    container_name: pitstop-redis
    restart: unless-stopped
    command: redis-server --requirepass ${REDIS_PASSWORD} --appendonly yes --maxmemory 512mb --maxmemory-policy allkeys-lru
    volumes:
      - /opt/pitstop/data/redis:/data
    ports:
      - "127.0.0.1:6379:6379"
    networks:
      - pitstop-app-network
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  #--- PITSTOP BACKEND ---
  pitstop-backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: pitstop-backend:latest
    container_name: pitstop-backend
    restart: unless-stopped
    environment:
      DATABASE_URL: ${DATABASE_URL}
      DATABASE_USERNAME: ${DATABASE_USERNAME}
      DATABASE_PASSWORD: ${DATABASE_PASSWORD}
      REDIS_HOST: pitstop-redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
      APP_FRONTEND_URL: ${APP_FRONTEND_URL}
      APP_BASE_URL: ${APP_BASE_URL}
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS}
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE}
      APP_COOKIE_DOMAIN: ${APP_COOKIE_DOMAIN:-}
      PITSTOP_OFICINA_NOME: ${PITSTOP_OFICINA_NOME}
      PITSTOP_OFICINA_CNPJ: ${PITSTOP_OFICINA_CNPJ}
      PITSTOP_OFICINA_TELEFONE: ${PITSTOP_OFICINA_TELEFONE}
      PITSTOP_OFICINA_EMAIL: ${PITSTOP_OFICINA_EMAIL}
      EVOLUTION_API_URL: ${EVOLUTION_API_URL}
      EVOLUTION_API_KEY: ${EVOLUTION_API_KEY}
      MAIL_HOST: ${MAIL_HOST}
      MAIL_PORT: ${MAIL_PORT}
      MAIL_USERNAME: ${MAIL_USERNAME}
      MAIL_PASSWORD: ${MAIL_PASSWORD}
      MAIL_FROM_NAME: ${MAIL_FROM_NAME}
      MAIL_SMTP_AUTH: ${MAIL_SMTP_AUTH}
      MAIL_SMTP_SSL_ENABLE: ${MAIL_SMTP_SSL_ENABLE}
      MAIL_SMTP_STARTTLS_ENABLE: ${MAIL_SMTP_STARTTLS_ENABLE}
      JAVA_OPTS: "-XX:+UseContainerSupport -XX:MaxRAMPercentage=75.0 -XX:+UseG1GC -Duser.timezone=America/Sao_Paulo"
    ports:
      - "127.0.0.1:8080:8080"
    networks:
      - pitstop-app-network
    depends_on:
      postgres-pitstop:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/actuator/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 90s
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 1G

  #--- PITSTOP FRONTEND ---
  # IMPORTANTE: DOMAIN_API deve ser app.pitstopai.com.br (mesmo domínio do frontend)
  # para que cookies funcionem corretamente em dispositivos mobile
  pitstop-frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        # IMPORTANTE: Usar https:// (não wss://) para WebSocket - SockJS faz upgrade automaticamente
        VITE_API_URL: https://${DOMAIN_API}/api
        VITE_WS_URL: https://${DOMAIN_API}/ws
        VITE_API_BASE_URL: https://${DOMAIN_API}
    image: pitstop-frontend:latest
    container_name: pitstop-frontend
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:80"
    networks:
      - pitstop-app-network
    depends_on:
      - pitstop-backend
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost/health"]
      interval: 30s
      timeout: 5s
      retries: 3
    deploy:
      resources:
        limits:
          memory: 256M

networks:
  pitstop-app-network:
    driver: bridge

# Volumes são criados automaticamente em /opt/pitstop/data/
